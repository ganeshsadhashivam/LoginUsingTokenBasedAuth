{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@now/node"
    }
  ],

  "routes": [{ "src": "/product/(?<id>[^/]+)", "dest": "/api/product?id=$id" }],

  "rewrites": [{ "source": "/product/:id", "destination": "/api/product" }]
}
